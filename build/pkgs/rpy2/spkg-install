#!/bin/sh

cp patches/setup.py src/

cd src

RHOMES="$SAGE_LOCAL"/lib/R; export RHOMES

python setup.py install

if [ $? -ne 0 ]; then
   echo "Error building RPY -- Python interface to R."
   exit 1
fi
