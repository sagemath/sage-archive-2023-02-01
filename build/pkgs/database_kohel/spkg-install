#!/usr/bin/env bash

if [ -z "$SAGE_LOCAL" ]; then
   echo "SAGE_LOCAL undefined ... exiting";
   echo "Maybe run 'sage -sh'?"
   exit 1
fi

cd src

TARGET="${SAGE_SHARE}/kohel/"
rm -rf ${TARGET}
mkdir ${TARGET}
mv PolMod ${TARGET}
mv PolHeeg ${TARGET}

