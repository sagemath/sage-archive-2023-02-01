LIBDIR=$(PREFIX)/lib
INCLUDEDIR=$(PREFIX)/include
DOCDIR=$(PREFIX)/doc

libflint.dylib: all
	$(CXX) -single_module -fPIC -dynamiclib -o libflint.dylib $(FLINTOBJ) -lgmp -lm

libflint.dll: all
	$(CXX) -fPIC -shared -o libflint.dll $(FLINTOBJ) -lgmp -lm

libflint.so: all
	$(CXX) -fPIC -shared -fPIC -o libflint.so $(FLINTOBJ) -lgmp -lm
