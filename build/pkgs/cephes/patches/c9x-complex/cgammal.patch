--- ../src/cgammal.c	2004-08-01 06:12:23.000000000 -0700
+++ cgammal.c	2010-02-15 23:05:28.478417600 -0800
@@ -317,7 +317,8 @@
 	    w = clgaml(1.0L - x);
 	    c = LOGPIL - clogl( u ) - w;
 	    /* Adjust for reduced sine arg.  */
-	    cimagl(c) += PIL * p;
+	    //cimagl(c) += PIL * p;
+	    (long double)(__imag__ c = cimagl(c) + PIL * p);
 	  }
 	goto ldone;
 	}
