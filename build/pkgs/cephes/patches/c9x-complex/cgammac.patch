--- ../../src/c9x-complex/cgamma.c	2010-04-06 02:46:28.000000000 -0700
+++ cgamma.c	2010-04-06 02:47:00.000000000 -0700
@@ -258,7 +258,7 @@
 	    w = clgam(1.0 - x);
 	    c = LOGPI - clog( u ) - w;
 	    /* Adjust for reduced sine arg.  */
-	    cimag(c) += PI * p;
+	    (double)(__imag__ c = (cimag(c) + PI * p));
 	  }
 	goto ldone;
 	}
