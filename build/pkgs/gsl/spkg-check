#!/usr/bin/env python

import os, sys

os.system("echo 'cd src; make check > test.err'")

r = open('test.err').read()

if 'FAIL' in r:
    open('test.out').read()
    print r
    sys.exit(1)
else:
    os.unlink('test.err')
    os.unlink('test.out')
    sys.exit(0)
