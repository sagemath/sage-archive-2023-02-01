cd src

./configure --prefix="$SAGE_LOCAL"

if [ $? -ne 0 ]; then
    echo "Error configuring."
    exit 1
fi

$MAKE install

if [ $? -ne 0 ]; then
    echo "Error building and installing."
    exit 1
fi
