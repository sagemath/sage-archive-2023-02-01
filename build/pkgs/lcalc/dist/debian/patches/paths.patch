Index: lcalc-20070107.p0/src/Makefile
===================================================================
--- lcalc-20070107.p0.orig/src/Makefile	2008-01-03 16:08:09.000000000 -0500
+++ lcalc-20070107.p0/src/Makefile	2008-01-03 16:08:14.000000000 -0500
@@ -21,7 +21,7 @@
 #multiprecision not yet fully implemented, so don't uncommment
 #PREPROCESSOR_DEFINE = -DUSE_MPFR
 ifeq ($(PREPROCESSOR_DEFINE),-DUSE_MPFR)
-  GMP_FLAGS= -L/usr/local/lib -lmpfr -lgmpxx -lgmp
+  GMP_FLAGS= -L/usr/lib -lmpfr -lgmpxx -lgmp
 else
   GMP_FLAGS=
 endif
@@ -57,7 +57,7 @@
 ifeq ($(PARI_DEFINE),-DINCLUDE_PARI)
     #location of pari.h.
     #LOCATION_PARI_H = /usr/local/include/pari #usual location
-    LOCATION_PARI_H = /usr/local/sage-1.1.0/local/include/pari #for sage
+    LOCATION_PARI_H = /usr/include/pari
     #LOCATION_PARI_H = /sw/include/pari #usual location
     #LOCATION_PARI_H = /usr/local/pari/pari-2.1.5/include/pari  #for caedmon
 
@@ -65,7 +65,7 @@
     #depending on whether static or dynamic libraries are being used.
     #On mac os x it's the former, on linux I think usually the latter.
     #LOCATION_PARI_LIBRARY = /usr/local/lib #usual location
-    LOCATION_PARI_LIBRARY = /usr/local/sage-1.1.0/local/lib #sage
+    LOCATION_PARI_LIBRARY = /usr/lib
     #LOCATION_PARI_LIBRARY = /sw/lib #usual location
     #LOCATION_PARI_LIBRARY = /usr/local/pari/pari-2.1.5/lib #for caedmon
 else
@@ -119,7 +119,7 @@
 #become clear which libraries the computer can find.
 
 
-INSTALL_DIR= /usr/local
+INSTALL_DIR= /usr
 
 #object files for the libLfunction library
 OBJ_L = Lglobals.o Lgamma.o Lriemannsiegel.o
Index: lcalc-20070107.p0/src/Makefile.sage
===================================================================
--- lcalc-20070107.p0.orig/src/Makefile.sage	2008-01-03 16:09:44.000000000 -0500
+++ lcalc-20070107.p0/src/Makefile.sage	2008-01-03 16:10:07.000000000 -0500
@@ -1,7 +1,7 @@
 lcalc: cmdline.c
 	$(CXX) $(CFLAGS) -DINCLUDE_PARI   $(DEFINES)\
-      -I$(SAGE_LOCAL)/include/pari -I$(SAGE_LOCAL)/include\
-      -I ../include/ -L$(SAGE_LOCAL)/lib \
+      -I/usr/include/pari -I/usr/include\
+      -I ../include/ -L/usr/lib \
       cmdline.c \
       Lcommandline.cc Lcommandline_elliptic.cc Lcommandline_globals.cc \
       Lcommandline_misc.cc Lcommandline_numbertheory.cc \
