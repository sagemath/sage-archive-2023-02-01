#!/usr/bin/env bash

if [ "$SAGE_LOCAL" = "" ]; then
   echo "SAGE_LOCAL undefined ... exiting";
   echo "Maybe run 'sage -sh'?"
   exit 1
fi

rm -rf "$SAGE_DATA"/reflexive_polytopes
mv src "$SAGE_DATA"/reflexive_polytopes
