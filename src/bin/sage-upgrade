#!/usr/bin/env bash

sage-update

if [ $? -ne 0 ]; then
    echo "Error getting new packages!"
    exit 1
fi

cd $SAGE_ROOT

cd $SAGE_ROOT/spkg/



./install

if [ $? -ne 0 ]; then
    echo "Error installing Sage!"
    exit 1
fi

cd $SAGE_ROOT
