--- src/sphinx/environment.py	2009-09-03 14:59:24.000000000 -0700
+++ patches/environment.py	2009-12-30 07:08:04.165172354 -0800
@@ -467,10 +467,13 @@
         else:
             # check if a config value was changed that affects how
             # doctrees are read
+            import inspect
             for key, descr in config.values.iteritems():
                 if descr[1] != 'env':
                     continue
                 if self.config[key] != config[key]:
+                    if inspect.isfunction(config[key]):
+                        continue
                     msg = '[config changed] '
                     config_changed = True
                     break
