#!/usr/bin/env bash

cd $SAGE_ROOT/devel/sage/doc/output/


grep -i "$@" *.html  2>/dev/null
grep -i "$@" */*.html 2>/dev/null
grep -i "$@" */*/*.html  2>/dev/null
grep -i "$@" */*/*/*.html  2>/dev/null
grep -i "$@" */*/*/*/*.html  2>/dev/null
grep -i "$@" */*/*/*/*/*.html  2>/dev/null
grep -i "$@" */*/*/*/*/*/*.html  2>/dev/null
grep -i "$@" */*/*/*/*/*/*/*.html  2>/dev/null
grep -i "$@" */*/*/*/*/*/*/*/*.html  2>/dev/null

