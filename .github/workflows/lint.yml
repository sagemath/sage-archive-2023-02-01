name: Lint

on: [push, pull_request]

jobs:
  lint-pycodestyle:
    name: Code style checking with pycodestyle
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Lint using pycodestyle
      uses: ricardochaves/python-lint@v1.2.0
      with:
        python-root-list: "src"
        use-pycodestyle: true
        extra-pycodestyle-options: --ignore=C0326 # C,E1,E2,E3,E402,E5,E9,W1,W2,W3,W4,W5   # all except for E401, E701, E702, W605
  lint-pyright:
    name: Static type checking with Pyright
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Set up node to install pyright
      uses: actions/setup-node@v1
      with:
        node-version: '12'
    - name: Install pyright
      run: npm install -g pyright
    - name: Lint using pyright
      run: pyright
