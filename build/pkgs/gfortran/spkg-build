. ./set-library-path

# #29241: gfortran on 32bit: Configure for the same ABI as gcc
ARCH=`$CC -print-multiarch`
case `uname -m`,"$ARCH" in
    x86_64,i386*)
        EXTRA_GCC_CONFIGURE="--build=$ARCH"
        ;;
esac

./build-gcc --disable-bootstrap --enable-languages=fortran $EXTRA_GCC_CONFIGURE
