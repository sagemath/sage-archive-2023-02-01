cd src

# Disable features that require packages SCIP and CoCoA, both of which
# are experimental packages.

# FLINT is a standard package.  We pass --enable-flint to configure so
# that an error will be signalled if FLINT cannot be found, rather
# than building normaliz without it.

sdh_configure --disable-scip --disable-nmzintegrate --enable-flint
sdh_make
sdh_make_install
